/** @format */

import type { ThemeConfig } from 'antd';

export const theme: ThemeConfig = {
	token: {},
	components: {
		Button: {
			borderRadius: 4,
		},
		Input: {
			borderRadius: 4,
		},
		InputNumber: {
			borderRadius: 4,
		},
	},
};
